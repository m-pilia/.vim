return {
    'tpope/vim-endwise',

    dependencies = {
        -- vim-endwise will remap <cr>
        -- ensure the nvim-autopairs <cr> mapping is set first
        'windwp/nvim-autopairs',
    },
}
